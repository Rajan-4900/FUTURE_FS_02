# Local dev: Vite proxies /api to localhost:5000 (see vite.config.js)
# VITE_API_URL=http://localhost:5000/api

# Vercel production: use same-origin /api (default) and set API_URL in Vercel dashboard
# so middleware can proxy to Render, e.g.:
# API_URL=https://your-crm-api.onrender.com/api
